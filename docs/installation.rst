.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install querv

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv querv
    $ pip install querv
